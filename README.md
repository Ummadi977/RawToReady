# RawToReady
RawToReady — AI-powered data pipeline that scrapes public websites, extracts tables from PDFs and Excel files, cleans and normalizes the data, and validates the output — all through a conversational Streamlit interface.
